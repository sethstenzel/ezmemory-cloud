[project]
name = "ezmemory"
version = "0.1.0"
description = "Note-taking and spaced-repetition study service"
requires-python = ">=3.12"
dependencies = [
    "argon2-cffi>=25.1.0",
    "django>=6.0",
    "markdown>=3.10",
    "pywebpush>=2.3.0",
]
